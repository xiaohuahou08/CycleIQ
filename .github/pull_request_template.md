## Summary

- 

## Test plan

- [ ] Web: `cd apps/web && npm test` (if present) / `npm run lint` / `npm run build`
- [ ] Python: `python -m unittest discover -s tests -t . -p "test_*.py"`
- [ ] Manual: seed demo data and verify terminal pages

## Screenshots (UI)

<!-- attach screenshots for UI changes -->

## Supabase migrations

- [ ] No DB changes
- [ ] DB changes included (describe migration + rollback strategy)

